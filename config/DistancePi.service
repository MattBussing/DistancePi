[Unit]
  Description=DistancePi start up at the beginning
  After=multi-user.target

[Service]
  Type=idle
  ExecStart=/bin/bash -c /home/DistancePi/start_up.sh
  RemainAfterExit=yes

[Install]
  WantedBy=multi-user.target
